# --- Django ---
DJANGO_SECRET_KEY=CHANGE_ME
DEBUG=False
ALLOWED_HOSTS=your.domain.com,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://your.domain.com
TIME_ZONE=Europe/Brussels

# --- Database ---
# Example for Postgres
DATABASE_URL=postgres://blockshelf:CHANGE_ME@localhost:5432/blockshelf
# (URL-encode special characters in the password)

# --- Security flags (prod over HTTPS behind a proxy) ---
SECURE_SSL_REDIRECT=True
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True

# --- Auth / Allauth ---
ACCOUNT_EMAIL_VERIFICATION=none
ACCOUNT_EMAIL_REQUIRED=false

# --- Email (console by default; switch to SMTP in production) ---
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@example.com
# For SMTP later:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.yourprovider.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=youruser
# EMAIL_HOST_PASSWORD=yourpass
# EMAIL_USE_TLS=True

# --- Optional integrations (can be set via GUI) ---
REBRICKABLE_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
